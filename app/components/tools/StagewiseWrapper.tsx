'use client';

import { StagewiseToolbar } from "@stagewise/toolbar-next";

export default function StagewiseWrapper() {
  return (
    <StagewiseToolbar
      config={{
        plugins: [],
      }}
    />
  );
} 